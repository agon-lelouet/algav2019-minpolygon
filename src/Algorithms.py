import Geometry as geo
import numpy as np

def GrahamAlgorithm(dataset: list) -> list:
    #this is where the magic happens
    return []


def ToussaintAlgorithm(convexHull: list) -> list:
    #here too
    return []
